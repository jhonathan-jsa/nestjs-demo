apiVersion: v1
kind: Service
metadata:
  name: nestjs-service
  labels:
    app: nestjs-service
  namespace: nestjs
spec:
  ports:
  - port: 443
    protocol: TCP
    targetPort: 3000
  selector:
    app: nestjs-service
  type: ClusterIP